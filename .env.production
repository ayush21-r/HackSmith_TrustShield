# Production Deployment Environment Variables

# Frontend environment variables for Vercel
# Set these in Vercel Project Settings → Environment Variables

# API URL - Set to your Render backend URL (without /api suffix)
# CRITICAL: Must be set for production login and API calls to work
VITE_API_URL=https://hacksmith-trustshield.onrender.com

# Optional: Base URL for API paths (not usually needed if VITE_API_URL is set correctly)
# VITE_API_BASE_URL=https://hacksmith-trustshield.onrender.com/api

# Backend environment variables for Render
# Set these in Render dashboard → Settings → Environment

# PostgreSQL database connection string from Supabase
# Format: postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
# DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres

# JWT Secret for token signing (keep this secret!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=your-prod-secret-key

# Server configuration
# PORT=5000
# NODE_ENV=production

# Optional: Frontend URL for CORS whitelisting
# FRONTEND_URL=https://your-frontend-domain.com
