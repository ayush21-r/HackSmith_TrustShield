# NEVER edit this file with real secrets - this is just a template!
# Copy this file to .env and fill in your actual values
# IMPORTANT: .env is in .gitignore and will NOT be committed

# ============================================================
# SECURITY: DO NOT COMMIT .env TO GIT
# ============================================================

# PostgreSQL Database Connection
# Get from Supabase: Project Settings → Database → Connection String
# Format: postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
DATABASE_URL="postgresql://postgres:your_supabase_password@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres"

# JWT Secret for authentication tokens
# Generate a random string - MUST be different in production
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-random-jwt-secret-change-this-in-production"

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# Frontend URL for CORS (optional)
# Set this if frontend is on a custom domain
# FRONTEND_URL=https://your-frontend-domain.com
